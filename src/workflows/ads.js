export async function draftEngagementAd({ message }) {
  // Safe no-op for now; wire to Meta later
  console.log('draftEngagementAd (stub):', message);
  return [];
}

export default { draftEngagementAd };
